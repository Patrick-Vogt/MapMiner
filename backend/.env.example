# Backend Configuration Example
# Copy this file to .env and adjust values as needed

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=True

# Server Configuration
HOST=0.0.0.0
PORT=5000

# CORS Configuration (for production, specify exact origins)
CORS_ORIGINS=*
